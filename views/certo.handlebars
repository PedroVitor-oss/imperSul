<style>
    body{
        display:flex;
        align-items: center;
        justify-content: center;
        background:#2E66FF;
        flex-direction:column;
        min-height:90vh;

    }
    button{
     background: yellow;
    border: 2px solid yellow;
    color: black;
    border-radius: 10px;
    height: 30px;
    }
    form{
        margin-top:20px;
    }
</style>
<h1>tudo certo</h1>
<form action="/home" method="POST">
<button type="subnit">ir para home</button>
</form>
<script>
     let urlParams = new URLSearchParams(window.location.search);
      let venda = urlParams.get("venda");
      let id = urlParams.get("id");
      if(venda == 'true')
      document.querySelector('body').innerHTML+=`
        <form action="/cadastraPedido?id=`+id+`" method="POST">
        <button type="subnit">mais um item</button>
        </form>
      `

</script>